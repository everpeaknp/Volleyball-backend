from django.apps import AppConfig

class ContactOptionsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.contact_options'
    verbose_name = '09: Contact Page'
